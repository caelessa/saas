# Implantação RC 1.0.1

Substitua o projeto pelos arquivos desta pasta, preservando segredos no Render. A nova tabela `signature` é criada automaticamente por `db.create_all()` na inicialização. Nenhuma variável de ambiente nova é necessária; o armazenamento usa as mesmas credenciais do Cloudflare R2.
